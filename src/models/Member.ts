export default interface Member {
    id : number,
    group : string,
    name : string,
    kana : string,
    birthdate : string,
    prefecture : string,
    height : string,
    bloodType : string,
    generation : number
}